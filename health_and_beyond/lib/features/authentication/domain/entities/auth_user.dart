class AuthUser {
  final String type, uid, email, name, city, state;

  AuthUser({
    required this.type,
    required this.uid,
    required this.email,
    required this.name,
    required this.city,
    required this.state,
  });
}
